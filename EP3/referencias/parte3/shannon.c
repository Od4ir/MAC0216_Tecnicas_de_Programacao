#include "shannon.h"

/* É para ser uma biblioteca estática. Para compilar e gerar o .so:
 * gcc -c -fPIC -o shannon.o shannon.c
 * gcc -o libshannon.so -shared shannon.o
 */ 
long ep3CalculaEntropiaShannon(char * stringEntrada, int base) {
    return 4;
}
